ion-view(ng-attr-title="{{homeLabel}}")
    ion-nav-buttons(side="left")
      a.button.icon.ion-ios7-home-outline(ui-sref="tab.info") 
    ion-content.has-header
        ion-slide-box(ng-if="resort.maps.length !== 0" does-continue="true")
            ion-slide.box(ng-repeat="map in resort.maps")
                img(width="100%" ng-src="{{map.src}}" style="margin-top: 20px")
