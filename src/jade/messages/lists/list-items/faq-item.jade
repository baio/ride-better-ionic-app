extends ./_item-layout.jade

block item-layout-content
  h3(ng-click='openThread(thread._id)') 
    i.ion-help-circled.calm &nbsp; 
    | {{::thread.data.shortText}}
  h4.dark(ng-click='openThread(thread._id)') {{::thread.replies[0].data.shortText}}

